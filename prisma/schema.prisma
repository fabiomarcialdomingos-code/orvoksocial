generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

// Fase 0: deliberately no product models. Fase 1 defines the approved schema.
